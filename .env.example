NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: switch the test backend from local SQLite to Supabase.
# Keep this key server-side only. Do not expose it as NEXT_PUBLIC_*.
TOPFACULTY_BACKEND=supabase
SUPABASE_URL=https://syekpeiharthpszexdry.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-your-service-role-key

# Optional local fallback path.
TOPFACULTY_DB_PATH=data/topfaculty.sqlite
